# 🚀 快速部署指南 - http://47.94.146.148:8888/

## ⚡ 一键部署（Windows）

### 方式 1: 批处理脚本（最简单）
```bash
.\scripts\部署到服务器.bat
```

### 方式 2: PowerShell 脚本（功能更全）
```powershell
.\scripts\部署到服务器.ps1
```

## 📋 手动部署步骤

### 1. 安装依赖
```bash
npm install
```

### 2. 构建项目
```bash
npm run build
```

### 3. 启动服务器
```bash
# Windows
set PORT=8888
node server.js

# Linux/Mac
PORT=8888 node server.js
```

## 🐳 Docker 部署（推荐生产环境）

```bash
cd docker
docker-compose build
docker-compose up -d
```

访问: http://47.94.146.148:8888/

## 🔧 配置说明

### 已修改的配置
- ✅ `vite.config.ts`: base 路径改为 `/`
- ✅ `server.js`: 端口改为 `8888`
- ✅ `docker/docker-compose.yml`: 端口映射改为 `8888:80`

### 环境变量
- `PORT`: 服务器端口（默认 8888）
- `DEEPSEEK_API_KEY`: DeepSeek API 密钥（在 server.js 中配置）

## 📝 部署检查清单

- [ ] Node.js 已安装（建议 18+）
- [ ] 依赖已安装（`npm install`）
- [ ] 项目已构建（`npm run build`）
- [ ] dist 目录存在
- [ ] 端口 8888 未被占用
- [ ] 防火墙已开放 8888 端口
- [ ] 服务器可访问 http://47.94.146.148:8888/

## 🛠️ 生产环境建议

### 使用 PM2 管理进程
```bash
npm install -g pm2
pm2 start server.js --name smart-qa -- --port 8888
pm2 startup
pm2 save
```

### 使用 Nginx 反向代理
```nginx
server {
    listen 8888;
    server_name 47.94.146.148;
    
    location / {
        proxy_pass http://localhost:8888;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## ❓ 常见问题

### 端口被占用
```bash
# Windows
netstat -ano | findstr :8888

# Linux/Mac
lsof -i :8888
```

### 构建失败
```bash
# 清除缓存重新安装
rm -rf node_modules package-lock.json
npm install
npm run build
```

### API 请求失败
- 检查 `server.js` 中的 API Key
- 检查网络连接
- 查看服务器日志

## 📞 需要帮助？

查看详细文档: `部署说明.md`

