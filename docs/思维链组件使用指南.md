# 思维链组件使用指南

> 让数字员工加入思维链进行思考，可视化展示 AI 的执行过程

---

## 📋 功能概述

思维链组件（ThoughtChain）用于可视化和追踪数字员工在执行任务时的思考过程、工具调用和操作步骤，让 AI 的"黑盒"过程变得透明可追踪。

### 核心特性

- ✅ **实时状态显示**：loading、success、error、abort 四种状态
- ✅ **可折叠内容**：支持展开/折叠查看详细步骤
- ✅ **嵌套结构**：支持多层级思维链（主任务 → 子任务 → 操作）
- ✅ **自定义图标**：支持自定义图标或使用默认状态图标
- ✅ **闪动效果**：支持 blink 效果突出当前执行步骤
- ✅ **连接线**：可视化展示步骤之间的关联

---

## 🎯 使用场景

### 1. 工具调用追踪

展示数字员工调用各种工具的过程：

```json
[thought-chain:{
  "items": [
    {
      "key": "step-1",
      "title": "知识库查询",
      "description": "查询知识库获取相关信息",
      "status": "success",
      "collapsible": true,
      "content": "从知识库中找到了3条相关记录"
    },
    {
      "key": "step-2",
      "title": "网页搜索工具调用",
      "description": "工具调用",
      "status": "loading",
      "collapsible": true,
      "content": "正在搜索最新的市场行情数据..."
    }
  ]
}]
```

### 2. 文件操作追踪

展示创建、修改文件的过程：

```json
[thought-chain:{
  "items": [
    {
      "key": "create-task",
      "title": "创建任务：编写新组件",
      "description": "执行创建新组件所需的文件",
      "status": "success",
      "collapsible": true,
      "children": [
        {
          "key": "create-folder",
          "title": "创建文件夹",
          "description": "为新组件创建文件夹",
          "status": "success",
          "content": "执行命令 `mkdir -p component`"
        },
        {
          "key": "create-files",
          "title": "创建文件",
          "description": "创建组件所需文件",
          "status": "success",
          "children": [
            {
              "key": "file-1",
              "title": "创建文件",
              "content": "component/index.tsx",
              "status": "success"
            },
            {
              "key": "file-2",
              "title": "创建文件",
              "content": "component/index.zh-CN.md",
              "status": "success"
            }
          ]
        }
      ]
    }
  ]
}]
```

### 3. 数据分析流程

展示数据分析的完整思考过程：

```json
[thought-chain:{
  "items": [
    {
      "key": "analyze",
      "title": "分析任务，理解任务工作流",
      "status": "success"
    },
    {
      "key": "query",
      "title": "查询数据库",
      "description": "执行SQL查询获取销售数据",
      "status": "success",
      "content": "SELECT SUM(amount) FROM sales WHERE date >= '2024-01-01'"
    },
    {
      "key": "visualize",
      "title": "生成可视化图表",
      "description": "基于查询结果生成折线图",
      "status": "success"
    },
    {
      "key": "insight",
      "title": "生成业务洞察",
      "description": "分析数据趋势并提供建议",
      "status": "loading",
      "blink": true
    }
  ]
}]
```

---

## 📊 数据结构

### ThoughtChainItem 接口

```typescript
interface ThoughtChainItem {
  key: string;                    // 唯一标识（必需）
  title: React.ReactNode;         // 标题（必需）
  description?: React.ReactNode;   // 描述（可选）
  content?: React.ReactNode;       // 内容（可选，可折叠显示）
  footer?: React.ReactNode;        // 脚注（可选）
  icon?: React.ReactNode | false;  // 图标（可选，false 时不显示）
  status?: 'loading' | 'success' | 'error' | 'abort';  // 状态（可选）
  collapsible?: boolean;           // 是否可折叠（默认 false）
  blink?: boolean;                 // 闪动效果（可选）
  children?: ThoughtChainItem[];  // 子节点（可选，支持嵌套）
}
```

### ThoughtChainProps 接口

```typescript
interface ThoughtChainProps {
  items: ThoughtChainItem[];           // 思维节点集合（必需）
  defaultExpandedKeys?: string[];      // 初始化展开的节点（可选）
  expandedKeys?: string[];             // 受控的展开节点（可选）
  onExpand?: (keys: string[]) => void; // 展开变化回调（可选）
  line?: boolean | 'solid' | 'dashed' | 'dotted';  // 连接线样式（默认 'dashed'）
  className?: string;                   // 自定义类名（可选）
}
```

---

## 🎨 UI设计规范

### 布局结构

```
┌─────────────────────────────────┐
│  ● [步骤1标题]                   │  ← 主节点（带图标）
│    [步骤1描述]                   │
│    ▼ [展开内容]                  │  ← 可折叠内容
│      • [子步骤1]                 │  ← 子节点（嵌套）
│      • [子步骤2]                 │
│    ───────────────────────────  │  ← 脚注（可选）
│    [脚注内容]                    │
└─────────────────────────────────┘
```

### 样式规范

- **容器**：
  - 背景：`bg-white`
  - 边框：`border border-[#E5E5EA]`
  - 圆角：`rounded-xl`
  - 阴影：`shadow-sm`
  - 内边距：`p-5`

- **连接线**：
  - 默认：`border-dashed border-[#007AFF]/30`（虚线，30% 透明度）
  - 实线：`border-solid border-[#007AFF]`
  - 点线：`border-dotted border-[#007AFF]/30`
  - 隐藏：`line={false}`

- **状态图标**：
  - loading：蓝色旋转图标 `Loader2`
  - success：绿色对勾 `CheckCircle2`
  - error：红色叉号 `XCircle`
  - abort：灰色警告 `AlertCircle`
  - 默认：蓝色圆点 `Circle`

- **标题**：
  - 字体大小：`text-[14px]`
  - 颜色：`text-[#1d1d1f]`
  - 字重：`font-medium`
  - 错误状态：`text-[#FF3B30]`
  - 成功状态：`text-[#34C759]`

- **描述**：
  - 字体大小：`text-[12px]`
  - 颜色：`text-[#86868b]`

- **内容**：
  - 字体大小：`text-[13px]`
  - 颜色：`text-[#646A73]`

---

## 💻 代码示例

### 示例 1: 基础用法

```typescript
const thoughtChainData = {
  items: [
    {
      key: 'step-1',
      title: '知识查询',
      description: '查询知识库',
      status: 'success'
    },
    {
      key: 'step-2',
      title: '网页搜索工具调用',
      description: '工具调用',
      status: 'loading',
      blink: true
    },
    {
      key: 'step-3',
      title: '模型调用完成',
      description: '调用模型生成响应',
      status: 'success'
    },
    {
      key: 'step-4',
      title: '响应完成',
      description: '任务完成',
      status: 'success'
    }
  ]
};
```

### 示例 2: 嵌套结构

```typescript
const nestedThoughtChain = {
  items: [
    {
      key: 'main-task',
      title: '创建任务：编写新组件',
      description: '执行创建新组件所需的文件',
      status: 'success',
      collapsible: true,
      children: [
        {
          key: 'sub-1',
          title: '创建文件夹',
          description: '为新组件创建文件夹',
          status: 'success',
          content: '执行命令 `mkdir -p component`'
        },
        {
          key: 'sub-2',
          title: '创建文件',
          description: '创建组件所需文件',
          status: 'success',
          children: [
            {
              key: 'file-1',
              title: '创建文件',
              content: 'component/index.tsx',
              status: 'success'
            },
            {
              key: 'file-2',
              title: '创建文件',
              content: 'component/index.zh-CN.md',
              status: 'success'
            }
          ]
        }
      ]
    }
  ]
};
```

### 示例 3: 带状态和自定义图标

```typescript
const statusThoughtChain = {
  items: [
    {
      key: 'loading-step',
      title: '工具调用中',
      status: 'loading',
      description: '正在调用数据库查询工具',
      collapsible: true,
      content: '执行 SQL: SELECT * FROM sales WHERE date >= "2024-01-01"'
    },
    {
      key: 'success-step',
      title: '工具调用完成',
      status: 'success',
      description: '查询成功，返回 1250 条记录'
    },
    {
      key: 'error-step',
      title: '工具调用错误',
      status: 'error',
      description: '数据库连接超时',
      content: '错误信息: Connection timeout after 30s'
    },
    {
      key: 'custom-icon',
      title: '任务完成',
      icon: <span className="text-2xl">🎉</span>,  // 自定义图标
      status: 'success'
    }
  ]
};
```

---

## 🔧 DeepSeek API 返回格式

### 格式 1: 简单思维链

```json
[thought-chain:{
  "items": [
    {
      "key": "step-1",
      "title": "分析用户问题",
      "description": "理解用户意图",
      "status": "success"
    },
    {
      "key": "step-2",
      "title": "查询数据",
      "description": "从数据库获取相关数据",
      "status": "loading",
      "blink": true
    }
  ]
}]
```

### 格式 2: 嵌套思维链

```json
[thought-chain:{
  "items": [
    {
      "key": "main",
      "title": "创建任务：分析销售数据",
      "description": "执行数据分析任务",
      "status": "success",
      "collapsible": true,
      "children": [
        {
          "key": "query",
          "title": "查询数据库",
          "status": "success",
          "content": "SELECT SUM(amount) FROM sales"
        },
        {
          "key": "analyze",
          "title": "分析数据",
          "status": "success",
          "content": "计算同比增长率：+19.8%"
        }
      ]
    }
  ]
}]
```

### 格式 3: 完整示例（文件操作）

```json
[thought-chain:{
  "items": [
    {
      "key": "create-component",
      "title": "创建任务：编写新组件",
      "description": "执行创建新组件所需的文件",
      "status": "success",
      "collapsible": true,
      "children": [
        {
          "key": "folder",
          "title": "创建文件夹",
          "description": "为新组件创建文件夹",
          "status": "success",
          "content": "执行命令 `mkdir -p component`"
        },
        {
          "key": "files",
          "title": "创建文件",
          "description": "创建组件所需文件",
          "status": "success",
          "children": [
            {
              "key": "index-tsx",
              "title": "创建文件",
              "content": "component/index.tsx",
              "status": "success"
            },
            {
              "key": "zh-cn-md",
              "title": "创建文件",
              "content": "component/index.zh-CN.md",
              "status": "success"
            },
            {
              "key": "en-us-md",
              "title": "创建文件",
              "content": "component/index.en-US.md",
              "status": "success"
            }
          ]
        }
      ],
      "footer": "所有文件创建完成"
    }
  ]
}]
```

---

## 🎯 实际应用场景

### 场景 1: 数据分析流程

```json
[thought-chain:{
  "items": [
    {
      "key": "understand",
      "title": "理解用户问题",
      "description": "分析用户意图：查询近3个月销售趋势",
      "status": "success"
    },
    {
      "key": "query",
      "title": "查询数据库",
      "description": "执行SQL查询",
      "status": "success",
      "collapsible": true,
      "content": "SELECT date, SUM(amount) as sales FROM sales WHERE date >= '2024-09-01' GROUP BY date"
    },
    {
      "key": "visualize",
      "title": "生成可视化图表",
      "description": "基于查询结果生成折线图",
      "status": "success"
    },
    {
      "key": "insight",
      "title": "生成业务洞察",
      "description": "分析趋势并提供建议",
      "status": "loading",
      "blink": true
    }
  ]
}]
```

### 场景 2: 工具调用链

```json
[thought-chain:{
  "items": [
    {
      "key": "knowledge-query",
      "title": "知识库查询",
      "description": "查询知识库获取相关信息",
      "status": "success"
    },
    {
      "key": "web-search",
      "title": "网页搜索工具调用",
      "description": "工具调用",
      "status": "loading",
      "blink": true,
      "content": "正在搜索：最新的市场行情数据"
    },
    {
      "key": "model-invoke",
      "title": "模型调用完成",
      "description": "调用模型生成响应",
      "status": "success"
    },
    {
      "key": "response",
      "title": "响应完成",
      "description": "任务完成",
      "status": "success"
    }
  ]
}]
```

### 场景 3: 错误处理

```json
[thought-chain:{
  "items": [
    {
      "key": "try-query",
      "title": "尝试查询数据库",
      "description": "执行SQL查询",
      "status": "error",
      "content": "错误：数据库连接超时",
      "collapsible": true,
      "children": [
        {
          "key": "retry",
          "title": "重试连接",
          "status": "loading"
        }
      ]
    }
  ]
}]
```

---

## 🛠️ 开发集成

### 1. 在 MessageBubble 中渲染

思维链组件已集成到消息系统中，会自动识别 `[thought-chain:{...}]` 格式并渲染。

### 2. 在 NarrativeText 中渲染

流式输出时，思维链会自动解析并实时渲染。

### 3. 手动使用组件

```typescript
import { ThoughtChain, ThoughtChainItem } from './components/ThoughtChain';

const items: ThoughtChainItem[] = [
  {
    key: 'step-1',
    title: '步骤1',
    description: '步骤描述',
    status: 'success'
  }
];

<ThoughtChain 
  items={items} 
  defaultExpandedKeys={['step-1']}
  line="dashed"
/>
```

---

## 📝 最佳实践

### 1. 状态管理

- **loading**：正在执行的步骤，使用 `blink: true` 突出显示
- **success**：成功完成的步骤
- **error**：执行失败的步骤，提供错误信息
- **abort**：被中止的步骤

### 2. 嵌套层级

- 建议不超过 3 层嵌套
- 主任务 → 子任务 → 具体操作

### 3. 可折叠设计

- 主要步骤设置为 `collapsible: true`
- 默认展开关键步骤：`defaultExpandedKeys: ['key1', 'key2']`

### 4. 实时更新

- 在流式输出过程中，逐步更新思维链状态
- 从 `loading` → `success` 或 `error`

---

## 🎨 自定义样式

### 修改连接线样式

```typescript
<ThoughtChain 
  items={items}
  line="solid"  // 'solid' | 'dashed' | 'dotted' | false
/>
```

### 自定义图标

```typescript
{
  key: 'custom',
  title: '自定义步骤',
  icon: <span className="text-2xl">🚀</span>,  // 自定义图标
  status: 'success'
}
```

### 隐藏图标

```typescript
{
  key: 'no-icon',
  title: '无图标步骤',
  icon: false,  // 不显示图标
  status: 'success'
}
```

---

## 🔍 调试技巧

### 1. 检查数据格式

确保 `items` 是数组，每个 item 都有 `key` 和 `title`：

```typescript
// ✅ 正确
items: [{ key: 'step-1', title: '步骤1' }]

// ❌ 错误
items: { key: 'step-1', title: '步骤1' }  // 应该是数组
```

### 2. 检查 JSON 格式

确保 DeepSeek 返回的 JSON 格式正确：

```json
// ✅ 正确
[thought-chain:{"items": [...]}]

// ❌ 错误
[thought-chain:{items: [...]}]  // 缺少引号
```

### 3. 状态更新

确保状态从 `loading` 正确过渡到 `success` 或 `error`。

---

## 📚 参考资源

- [Ant Design X ThoughtChain 文档](https://ant-design-x.antgroup.com/components/thought-chain-cn)
- 组件源码：`src/components/ThoughtChain.tsx`
- 类型定义：`src/components/ThoughtChain.tsx`

---

**最后更新**：2024-12-12
