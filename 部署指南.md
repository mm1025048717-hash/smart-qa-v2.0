# 部署指南 - GitHub Pages 和 Vercel

本指南将帮助你将项目部署到 GitHub Pages 和 Vercel。

## 📋 目录

1. [GitHub Pages 部署](#github-pages-部署)
2. [Vercel 部署](#vercel-部署)
3. [环境变量配置](#环境变量配置)
4. [常见问题](#常见问题)

---

## 🚀 GitHub Pages 部署

### 前置条件

- GitHub 账号
- 项目已推送到 GitHub 仓库

### 步骤 1: 启用 GitHub Pages

1. 访问你的 GitHub 仓库
2. 点击 **Settings**（设置）
3. 在左侧菜单中找到 **Pages**（页面）
4. 在 **Source**（源）部分：
   - 选择 **GitHub Actions** 作为部署源
   - 不要选择 "Deploy from a branch"
5. 保存设置

### 步骤 2: 配置 GitHub Secrets（可选）

如果需要使用 API Key，配置 GitHub Secrets：

1. 进入仓库 **Settings** → **Secrets and variables** → **Actions**
2. 点击 **New repository secret**
3. 添加以下 Secret：
   - **Name**: `VITE_DEEPSEEK_API_KEY`
   - **Value**: 你的 DeepSeek API Key（格式：`sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`）
4. 点击 **Add secret**

### 步骤 3: 推送代码

```bash
# 确保代码已提交
git add .
git commit -m "准备部署到 GitHub Pages"
git push origin main
```

### 步骤 4: 查看部署状态

1. 访问仓库的 **Actions** 标签页
2. 查看最新的工作流运行状态
3. 等待部署完成（通常需要 2-5 分钟）

### 步骤 5: 访问部署的网站

部署成功后，你的网站将可以通过以下 URL 访问：

```
https://[你的用户名].github.io/[仓库名]/
```

例如：`https://mm1025048717-hash.github.io/smart-qa-v2.0/`

> **注意**：GitHub Pages 是静态网站托管，无法运行后端服务。如果需要 API 代理功能，请使用 Vercel 部署。

---

## 🌐 Vercel 部署

### 前置条件

- Vercel 账号（可通过 GitHub 账号登录）
- 项目已推送到 GitHub 仓库

### 步骤 1: 连接 GitHub 仓库

1. 访问 [Vercel Dashboard](https://vercel.com/dashboard)
2. 点击 **Add New Project**（添加新项目）
3. 选择 **Import Git Repository**（导入 Git 仓库）
4. 选择你的 GitHub 仓库（`smart-qa-v2.0`）
5. 点击 **Import**（导入）

### 步骤 2: 配置项目设置

Vercel 会自动检测项目配置，确认以下设置：

- **Framework Preset**: Vite
- **Root Directory**: `./`（项目根目录）
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm ci`

### 步骤 3: 配置环境变量

在部署前，配置环境变量：

1. 在项目设置页面，找到 **Environment Variables**（环境变量）
2. 点击 **Add New**（添加新变量）
3. 添加以下环境变量：

| Key | Value | Environment |
|-----|-------|-------------|
| `DEEPSEEK_API_KEY` | 你的 DeepSeek API Key | Production, Preview, Development |
| `VITE_DEEPSEEK_API_KEY` | 你的 DeepSeek API Key（可选，如果前端需要） | Production, Preview, Development |

> **重要**：
> - `DEEPSEEK_API_KEY` 用于 Vercel Serverless Function（API 路由）
> - `VITE_DEEPSEEK_API_KEY` 用于前端构建时注入（如果需要在构建时使用）
> - 确保选择所有环境（Production、Preview、Development）

### 步骤 4: 部署

1. 点击 **Deploy**（部署）按钮
2. 等待部署完成（通常需要 1-3 分钟）
3. 部署成功后，Vercel 会提供一个预览 URL

### 步骤 5: 配置自定义域名（可选）

1. 在项目设置页面，找到 **Domains**（域名）
2. 点击 **Add Domain**（添加域名）
3. 输入你的域名
4. 按照提示配置 DNS 记录

---

## 🔑 环境变量配置

### GitHub Pages

GitHub Pages 使用 GitHub Secrets 配置环境变量：

1. 进入仓库 **Settings** → **Secrets and variables** → **Actions**
2. 添加 Secret：`VITE_DEEPSEEK_API_KEY`
3. 工作流会自动在构建时注入

### Vercel

Vercel 使用 Dashboard 配置环境变量：

1. 进入项目 **Settings** → **Environment Variables**
2. 添加以下变量：
   - `DEEPSEEK_API_KEY`（用于 API 路由）
   - `VITE_DEEPSEEK_API_KEY`（用于前端构建，可选）

### 获取 DeepSeek API Key

1. 访问 [DeepSeek Platform](https://platform.deepseek.com/)
2. 注册/登录账号
3. 进入控制台，找到 API Keys
4. 创建新密钥
5. 复制 API Key（格式：`sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`）

---

## 🔧 API 路由配置

### Vercel Serverless Function

项目已配置 Vercel Serverless Function 作为 API 代理：

- **文件位置**: `api/deepseek.js`
- **路由**: `/api/deepseek`
- **功能**: 代理 DeepSeek API 请求，处理 CORS 和 API Key

### 工作原理

1. 前端发送请求到 `/api/deepseek/chat/completions`
2. Vercel Serverless Function 接收请求
3. Function 使用环境变量中的 `DEEPSEEK_API_KEY` 调用 DeepSeek API
4. 返回响应给前端

### 配置检查清单

- [ ] `api/deepseek.js` 文件存在
- [ ] `vercel.json` 配置正确
- [ ] 环境变量 `DEEPSEEK_API_KEY` 已配置
- [ ] Vercel 项目已连接 GitHub 仓库

---

## 🐛 常见问题

### 问题 1: GitHub Pages 部署失败

**可能原因**：
- GitHub Pages 未启用
- 工作流权限未配置
- 构建错误

**解决方案**：
1. 检查仓库 Settings → Pages，确认已选择 GitHub Actions
2. 检查仓库 Settings → Actions → General，确认工作流权限已启用
3. 查看 Actions 标签页的构建日志，找出错误原因

### 问题 2: Vercel 部署后 API 返回 401 错误

**可能原因**：
- 环境变量未配置
- 环境变量名称错误
- 未重新部署

**解决方案**：
1. 检查项目 Settings → Environment Variables，确认 `DEEPSEEK_API_KEY` 已添加
2. 确认环境变量已应用到所有环境（Production、Preview、Development）
3. 重新部署项目（在 Deployments 页面点击 Redeploy）

### 问题 3: Vercel API 路由返回 404

**可能原因**：
- API 路由文件路径不正确
- `vercel.json` 配置错误

**解决方案**：
1. 确认 `api/deepseek.js` 文件存在
2. 检查 `vercel.json` 中的 rewrites 配置
3. 确认 API 路由路径为 `/api/deepseek`

### 问题 4: CORS 跨域错误

**可能原因**：
- GitHub Pages 直接调用 DeepSeek API 会遇到 CORS 问题
- API 路由未正确配置 CORS 头

**解决方案**：
- **GitHub Pages**: 使用 Vercel 部署（支持 Serverless Functions）
- **Vercel**: 检查 `api/deepseek.js` 中的 CORS 头配置

### 问题 5: 环境变量未生效

**可能原因**：
- 环境变量配置后未重新部署
- 环境变量名称错误（注意大小写）

**解决方案**：
1. 配置环境变量后，必须重新部署才能生效
2. 确认环境变量名称完全匹配（区分大小写）
3. 在 Vercel Dashboard 中检查环境变量是否已添加

---

## 📚 相关文档

- [GitHub Actions 文档](https://docs.github.com/en/actions)
- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [Vercel 文档](https://vercel.com/docs)
- [Vercel Serverless Functions](https://vercel.com/docs/functions)
- [DeepSeek API 文档](https://api-docs.deepseek.com/zh-cn/)

---

## ✅ 部署检查清单

### GitHub Pages

- [ ] GitHub Pages 已启用（使用 GitHub Actions）
- [ ] GitHub Secrets 已配置（如需要）
- [ ] 代码已推送到 main 分支
- [ ] Actions 工作流运行成功
- [ ] 网站可以正常访问

### Vercel

- [ ] 项目已连接到 GitHub 仓库
- [ ] 环境变量已配置（`DEEPSEEK_API_KEY`）
- [ ] API 路由文件存在（`api/deepseek.js`）
- [ ] `vercel.json` 配置正确
- [ ] 部署成功，网站可以正常访问
- [ ] API 路由可以正常调用（测试发送消息）

---

## 🎯 快速部署命令

### GitHub Pages

```bash
# 1. 提交代码
git add .
git commit -m "部署到 GitHub Pages"
git push origin main

# 2. 等待 GitHub Actions 自动部署
# 3. 访问 https://[用户名].github.io/[仓库名]/
```

### Vercel（使用 CLI）

```bash
# 1. 安装 Vercel CLI
npm i -g vercel

# 2. 登录 Vercel
vercel login

# 3. 在项目目录下部署
vercel

# 4. 生产环境部署
vercel --prod
```

---

## 🔐 安全建议

1. **不要将 API Key 提交到 Git**
   - 确保 `.env` 文件在 `.gitignore` 中
   - 不要在代码中硬编码 API Key

2. **使用环境变量**
   - GitHub Pages: 使用 GitHub Secrets
   - Vercel: 使用 Environment Variables

3. **定期轮换 API Key**
   - 定期更新 API Key
   - 更新后记得同步更新环境变量

4. **监控 API 使用**
   - 定期检查 DeepSeek 平台的 API 使用情况
   - 设置使用量告警

---

**提示**：如果遇到问题，请查看：
- GitHub Actions 日志（GitHub Pages）
- Vercel 部署日志（Vercel）
- 浏览器控制台错误信息
