# 🎤 TTS 服务选择指南

## 🎯 推荐方案：Deepgram TTS（已配置）⭐

**您已经有 Deepgram API Key，可以直接使用！无需额外配置！**

### 优势
- ✅ **与 STT 共用 API Key** - 无需额外获取
- ✅ **免费额度**: 每月 $200
- ✅ **低延迟**: 实时流式输出
- ✅ **高质量**: 自然语音

### 当前状态
- ✅ 已自动配置为使用 Deepgram TTS
- ✅ 无需 OpenAI API Key

---

## 其他可选 TTS 服务

### 1. Cartesia TTS ⭐⭐⭐⭐

**优势**: 高质量，免费额度 $10/月

**切换方法**:
```bash
切换TTS服务.bat cartesia
```

然后获取 API Key: https://play.cartesia.ai/sign-up

---

### 2. Piper TTS ⭐⭐⭐⭐⭐（完全免费）

**优势**: 完全免费，本地运行，无需 API Key

**切换方法**:
```bash
切换TTS服务.bat piper
```

**安装**:
```bash
pip install pipecat-ai[piper]
```

---

### 3. ElevenLabs TTS ⭐⭐⭐

**优势**: 最自然的语音，支持情感表达

**切换方法**:
```bash
切换TTS服务.bat elevenlabs
```

然后获取 API Key: https://elevenlabs.io/

---

### 4. OpenAI TTS ⭐⭐⭐

**优势**: 质量稳定

**切换方法**:
```bash
切换TTS服务.bat openai
```

然后获取 API Key: https://platform.openai.com/api-keys

---

## 快速切换

运行 `切换TTS服务.bat <服务名称>` 即可切换。

例如：
```bash
切换TTS服务.bat deepgram    # 使用 Deepgram（推荐）
切换TTS服务.bat cartesia    # 使用 Cartesia
切换TTS服务.bat piper        # 使用 Piper（免费）
```

---

## 当前推荐

**使用 Deepgram TTS** - 已自动配置，无需任何额外操作！

直接运行 `start.bat` 即可开始使用！


